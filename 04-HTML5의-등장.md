## 웹의 역할 변화
2000년대 중후반부터 웹이 수행하는 역할 자체가 근본적으로 바뀌기 시작했다. 
초창기 웹은 신문을 읽듯이 정적인 문서를 열람하는 공간에 가까웠다. 하지만 이 시간이 지나면서 웹은 이메일, 지도, 동영상 등 브라우저 안에서 돌아가는 애플리케이션 플랫폼으로 그 역할이 확장되었다.

하지만 당시 HTML4는 이런 변화를 뒷받침할 만큼 설계되어 있지 않았다. 
대표적으로 웹에서 동영상을 재생하려면 브라우저 자체 기능이 아니라 Flash와 같은 별도의 플러그인을 설치해야 했다. 이는 보안 취약점, 모바일 기기와의 호환성 문제, 무거운 리소스 사용 등 여러 부작용을 동반했다.

이런 문제점을 발견하고, HTML5는 아래와 같은 목표를 가지고 설계되었다.

| 목표 | 필요성 |
|------|------|
| 플러그인 없는 멀티미디어 재생 | Flash 의존 탈피 |
| 브라우저 내 그래픽/게임 렌더링 | JS로 직접 화면을 그릴 공간 필요 |
| 문서 구조의 의미 명확화 | <div> 남발로 인한 의미 손실 해소 |
| 앱에 준하는 기능 지원	| 오프라인 사용, 위치 기반 서비스 등 요구 증가	|

HTML5는 "단순 문서 열람 도구"에서 "웹 애플리케이션 플랫폼"으로 자연스럽게 전환되었다.

## 왜 아직도 HTML5일까?

2014년, HTML5는 W3C의 정식 표준으로 공식 권고되었다. 그런데 10년이 지난 지금까지도 웹 기술은 여전히 "HTML5"라는 이름표를 달고 있다. 왜 HTML6은 나오지 않는가?

WHATWG는 HTML5 이후로 버전 번호 체계 자체를 폐지했다.

### 1. 예전 방식 — 버전 모델(Versioned Model)
- 여러 해에 걸쳐 새로운 기능들을 논의하고 스펙 문서를 다듬는다.
- 모든 논의가 마무리되면 "이제 이 버전은 완성되었다"고 공식적으로 발표한다.
- 다음 버전을 위한 논의를 처음부터 다시 시작한다.

과거의 표준화 방식은 속도가 문제였다. 
웹 기술과 브라우저 기능은 하루가 다르게 발전하는데, 스펙이 "완성"되어 공식 발표되기까지 몇 년씩 걸리는 버전 모델로는 이 속도를 따라잡을 수 없었다. 
실제로 XHTML 2.0이 표류하는 동안, 현실의 브라우저 회사들은 이미 그보다 훨씬 실용적인 기능들을 독자적으로 구현하고 있었다는 점이 이 한계를 잘 보여준다.

### 2. 현재 방식 — Living Standard 모델

WHATWG는 이 문제를 해결하기 HTML 스펙 문서를 "영원히 미완성 상태로 남는 살아있는 문서(Living Standard)"로 정의했다.

이 모델에서는 다음과 같은 방식으로 표준이 갱신된다.

- 브라우저 회사가 새로운 기능을 실험적으로 구현한다.
- 다른 브라우저 회사들과의 논의를 거쳐 합의에 도달한다.
- 합의된 기능은 다음 버전을 기다릴 필요 없이 즉시 스펙 문서에 반영된다.

그 결과, "HTML5"라는 이름은 마케팅적·역사적 상징으로 남아있을 뿐, 실제 스펙 문서는 매달, 심지어 매주 조금씩 갱신되는 문서가 되었다. 버전이라는 개념 자체가 사실상 의미가 없어진 것이다.

### 3. XHTML의 실패와의 연결고리

방식의 전환은 XHTML의 실패와 연관되어 있다. XHTML은 이론적으로 완벽하고 엄격한 스펙을 지향하다가 현실의 웹 생태계와 괴리되어 실패했다.

이론적으로 완벽한 설계를 먼저 그려놓고 구현을 기다리는 것이 아니라,
실제로 브라우저에 구현되고, 실제로 쓰이는 기능을 표준화의 대상으로 삼는다.

즉 "완벽한 계획 → 구현"이 아니라 "구현과 합의 → 표준"으로 순서 자체가 뒤바뀐 것이다. 이는 웹 표준화의 무게중심이 이상적 설계에서 실용성과 현실의 웹 생태계 쪽으로 완전히 이동했음을 보여주는 상징적인 변화다.
